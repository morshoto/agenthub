# Changelog

Versioned release history for `agenthub`.

GitHub Releases: https://github.com/morshoto/agenthub/releases

## Unreleased - 2026-04-10

### Features
- the command and the main destroy tests (0e93947)
- the root wiring needs cleanup (ae394e2)
- code changes are in (c8fbd3d)
- command on this is wired (0ee40ed)
- fast path is unchanged and passing (7331ec6)
- the command targets are in place (6d5c2eb)
- the HTTP wiring is done (a8678ec)
- repo-level validation entrypoint (21979a8)
- a root golangci-lint config at .golangci.yml:1 with a conservative baseline: errcheck (9ddb89b)
- workflow file is in place (fd8de27)
- the update disclaimer for out-of-date CLI builds (edccc82)
- add homebrew install debug logs (299a117)
- homebrew formula generation script on github ation (28aa4b8)
- fix ssh connection (98f582b)

### Fixes
- one test helper mismatch in the new test file (a3122b7)
- Fix golangci-lint v2 schema (c07ebf9)
- Fix golangci-lint v2 config (1c4cb80)
- restore progress renderer compile path (92550a5)
- normalize wizard display formatting (4e3ee30)
- fix homebrew dry-run extraction (f29a40b)
- fix homebrew debug lint (b8a97b6)
- fix homebrew buildpath for dry run (b3e8f62)
- fix homebrew debug output (b628c1d)

### Documentation
- CONTRIBUTION.md (59d7480)
- Update README.md (0ab1d38)
- Revise README installation instructions (752b1fc)
- README update (8848daf)

### Maintenance
- destroy-specific failures are fixed (6abda76)
- the new assertions use slices.Equal and root_test.go doesn’t import slices yet (8f9497f)
- fix is in and the package test run is back in progress (fffe2bf)
- command and tests are in place (23899c2)
- formatting the touched files, then running targeted Go tests (7570620)
- the server and verification code are patched (2dd4b46)
- go-ci.yml into conventional GitHub Actions YAML (a03bd89)
- erasing duplicated github action [skip actions] (fcbdaf0)
- cli (e7470ee)
- Narrow initial golangci-lint policy (aca38c0)
- init no longer offers a network-mode choice (394695b)
- init, create, and infra tfvars now auto-use (df15573)
- a shared auth-recovery checks AWS auth and detects SSO-backed profiles (61f2074)
- logLine writes a single scroll-safe log line with a [phase] context prefix (1f833e4)
- progress spinner and tone (9304c78)
- TTY modifications (69c4b3b)
- Disable create progress in init (338e5da)
- Preserve CLI progress history (7bffdca)
- .gitignore contains .playwright-mcp (a35c932)
- conditional statement fix (0d8ea42)
- replace the shell ls -la debug with Ruby-side output (3c4562e)
- revert homebrew buildpath change (ba8294f)
- debugging steps (285078e)
- wrapped the Go build in cd buildpath do ... end in the formula renderer (faffa89)
- python file name update (a4a67a3)
- Homebrew renderer emit version only when needed (ecc2b80)
- the renderer to require --version and write version "{version}" into the formula (fbcb7ad)
- SSH readiness failures include elapsed time (497fe5f)
- livecheck on Formula/agenthub.rb (bb9df55)

### Internal
- infrastructure destroy test (bbb3d8b)
- test run is still in flight (072a530)
- command-level tests (7c088e4)
- failure was in the new redeploy tests (52a69d6)
- Delete .playwright-mcp directory (28691ea)

## v0.1.1 - 2026-04-06

### Features
- nix versioning with homebrew (ed9b483)
- nix and flake.nix package (2b8384c)

### Fixes
- release note publish state (f7b4aef)

### Maintenance
- adds concurrency and prevent publish at the same time (b3c186c)
- nix build with go_1_25 with linux installation guide (19511d0)
- nix flake build (207bafe)

## v0.1.0 - 2026-04-06

### Features
- fallback when there are no tags (7b316a2)
- Add GCP and Azure VM modules (af6080a)
- Add platform-aware infra scaffolding (527f87b)
- checking instance id, state, instance type, and other info (48aa925)
- setup detects a GitHub owner/repo candidate from git remote origin (1395a8d)
- project-owned GitHub App config under github (245e0aa)
- slack deploy and EC2 configuration (ae15afb)
- slack application bot deploy (fb046e1)
- slack connection and slack serve command (0bbec79)
- agent directories and listAgentConfigFiles (1ce3375)
- agent name (c35dc42)
- status flag (184bdb9)
- release and publish github action (4dc227c)
- daily dependabot setting (ec484d6)
- Connect GitHub now (f51474e)
- Add GitHub SSH setup to init (9e4f1ef)
- captures a GitHub SSH private key path and persists it in config via setup/wizard.go (ecc21a6)
- verification steps (8a5b020)
- BootstrapStatus (d35740e)
- aws bedrock connection to openclaw (7ce2a36)
- workflow provision and flag (2bc6f9f)
- Dockerfile on route with go binary file to run openclaw serve (13a5737)
- terraform application config (f5d923a)
- runtime install workflow (958cf6c)
- modiy open claw as always (56b17ec)
- ssh connection on app workflow (c1fdde3)
- aws profile and create resources (a04be21)
- lint job on github action (fd9af3d)
- cli-ci workflow (132a9e0)
- shared workflow orchestration for infra create, install, and verify, plus consistent (7d03082)
- Add runtime install workflow (430393e)
- Add remote host executor abstraction (049dd49)
- infra create validates SSH flags as a pair and resolves the AMI (794e187)
- Add EC2 infra create command (2904d76)
- ANSI styling in terminal sesisons (7c8bab8)
- Select now uses a raw terminal menu (1e3a733)
- use sts.GetCallerIdentity for AWS auth check with --profile (fb1a0c3)
- CloudProvider contract with GPU quota reporting types (f1f5dc8)
- AWS Provider Abstraction (4bea679)
- config file (ba31467)
- go module and cli entorypoint (e7b5e04)
- update .gitignore content (12621bc)
- github workflow and labeling files (98b7508)

### Fixes
- Fix searchable instance picker navigation (227edcc)
- threaded the mode bit into the core path (d85f918)
- falls back to the bundled AMI list when SSM returns an AWS auth-style failure (5a7ab2f)
- init keeps moving when AWS SSM image resolution fails (508d5be)
- aws auth issue (85454ed)

### Documentation
- README badges and install (912f66d)
- README and config (c846a66)
- README documents (153ed17)
- update README file (05ec376)
- README update (e4f4d15)
- changelog configuration (50081e2)

### Maintenance
- PR template (afeb32a)
- normalized the edited Go files (d0f6bbc)
- rerunning the same test slice now to confirm the metadata contract (6a19fe7)
- adding provider tests next so the AWS lookup path (5543d46)
- remove duplicate plan screen and grouped progress rendering (fd1cdae)
- renders a live snapshot instead of an append-only transcript in TTY mode (c74491f)
- test configuration file change (545c4a7)
- initi and create wizard with steps (4db66fa)
- clear the active line before returning from a cursor-menu selection (c10e912)
- route Select and SelectSearch through a width-aware cursor-menu check (7e8758d)
- handles the full flow end to end (218877a)
- modify module name and imports (901fcae)
- cli name to agenthub (d03ad22)
- doc organizing (78319c7)
- internal codexauth on EC2 insntace (ddc8a7f)
- writes the runtime config (641b277)
- ssh connection and display shorter message (5f61e58)
- ssh fix and instance preventation when pipeline fails (3a38b39)
- select aws profile from configuration file (29131b1)
- accept agent default name and asking first (2f0247d)
- status flag without config file (9f20fac)
- no longer asks for a separate GitHub SSH private key path (516deca)
- Running Dokcerfile on EC2 instance (0177786)
- renaming cli ci file into go ci [skip actions] (fc4f8ac)
- setup using terraform with default ssh key value (3f0b778)
- full test suite passes with a writable Go cache (21fd417)
- shake out the remaining test (9264570)
- terraform execution path (8bbac2e)
- SSH protocol and auto EC2 detection (7e60053)
- clean up (2309f59)
- full test suite passed (c130ea4)
- the AWS provider was still returning the GPU instance list even in CPU mode (6fec6de)
- changing the network menu order would silently change (bba25f2)
- minor naming change [skip actions] (e63dc80)
- aws configuration setup (2fd946d)
- uses AWS Service Quotas instead of the mock report (6a899d2)
- provider is generated only when AWS is being chosen from platform selection (eb120e4)
- default AMI as AWS Deep Learning GPU base and choose region (b7a157e)
- default instance type to be GPU (21fa435)
- Quota check hides real AWS values (1367e26)
- replace github.com/spf13/cobra with ./third_party/cobra override from go.mod (0544aba)
- wired config validate (5b7e67b)
- object oriented app structure (cace29e)
- separate code basis (15f2c28)

### Internal
- slack deploy tests (9a3c97f)
- running gofmt -w on files (8d1aa4d)
- last scope bug is fixed (7e20a31)
- Delete .playwright-mcp directory (9edbc7f)
- run time tests (ae774e4)
- installation tests (34d9a91)
- infra/aws/ec2/ (8517457)
- formatting go files (a3eef8e)
- needs to be updated for new orchestration suit (cbf3e56)
- running tests in determinicstic terraform path instead of mock (bb6a516)
- updating old test cases (e406eb5)
- a CPU-only init test (bc77610)
- remove YAML parser and serialiing and import gopkg.in/yaml.v3 (53b14ac)

### Other
- inventory agent configs (e5a433f)
- write agent configs to directories (eb373bf)
- scan agent configs (1f05b03)
- Skip Codex model prompt (36fcba8)
- Document Codex browser login flow (9650da6)
- Drop Codex API key requirement (b8096b9)
- Filter instance types by compute mode (89ea4b5)
- Show full AWS instance catalog (6ef5319)
- Prompt for AWS profile before lookup (ddec47f)
- Fetch AWS init options live (f5386e2)
- Scope source archive test stubs locally (f10d22c)
- Share terraform input preparation (6eb934c)
- Document host networking in user data (617732d)
- Clarify tfvars generation prerequisites (5fe4dd2)
- Make progress renderer context-aware (01f76ca)
- Remove unused source_ref plumbing (009a823)
- Initial commit (34c1c90)
